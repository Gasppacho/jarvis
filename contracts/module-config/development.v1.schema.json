{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Development Module Config v1",
  "description": "Validation and workspace behavior for one Development Module Instance.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "validationOrder",
    "maxRepairCycles"
  ],
  "properties": {
    "validationOrder": {
      "title": "Validation order",
      "description": "Checks run after implementation, in this order.",
      "type": "array",
      "default": [
        "lint",
        "typecheck",
        "test",
        "build"
      ],
      "examples": [
        [
          "lint",
          "typecheck",
          "test",
          "build"
        ]
      ],
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "title": "Validation check",
        "description": "A project command to run during validation.",
        "enum": [
          "lint",
          "typecheck",
          "test",
          "build"
        ]
      }
    },
    "maxRepairCycles": {
      "title": "Maximum repair cycles",
      "description": "Maximum implementation repair attempts after validation fails.",
      "type": "integer",
      "default": 2,
      "examples": [
        2
      ],
      "minimum": 0,
      "maximum": 5
    },
    "retainWorkspaceOnSuccess": {
      "title": "Retain successful workspace",
      "description": "Keep the temporary Git workspace after a successful execution.",
      "type": "boolean",
      "default": false,
      "examples": [
        false
      ]
    }
  }
}
