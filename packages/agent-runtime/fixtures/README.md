# Codex session fixture

`codex-cli-0.153.4-happy-path.jsonl` is a trimmed, decision-rich JSONL stream recorded from `codex-cli 0.153.4`. It keeps an interleaved command pair, including one non-zero command exit, alongside the message and file-change observations.
