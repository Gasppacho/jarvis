/** Kept in sync with apps/engine/package.json by version.test.ts. */
export const ENGINE_VERSION = "0.1.0";

/** The only API major this engine speaks. Mirrors the `/v1` prefix. */
export const API_VERSION = "v1" as const;
