packages:
  - apps/engine
  - packages/*
  - packages/modules/*

# better-sqlite3 ships the native addon; esbuild fetches its platform binary.
# Both are pinned, and the engine bundle loads the addon by explicit path.
allowBuilds:
  better-sqlite3: true
  esbuild: true
